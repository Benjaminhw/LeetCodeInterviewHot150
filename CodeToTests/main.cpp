#include "function.h"

int main()
{
	int* p = new int(28);
	for (int i = 0; i < 1; ++i)
	{
		vector<int> test1(16);
		//delete p;
		test1.push_back(*p);
	}
	//delete p;
	vector < Solution> test2(16);
	return 0;
}
